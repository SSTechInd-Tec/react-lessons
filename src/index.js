import React from 'react';
import ReactDOM from 'react-dom/client';
import GeneralComponent from './components/Organisms/GeneralCompnent/GeneralComponent';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <GeneralComponent />
  </React.StrictMode>
);
